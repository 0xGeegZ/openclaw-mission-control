# Mission Control Environment Variables
# Per Turbo docs: put .env files in the packages where they're used.
# - Web app: copy to apps/web/.env.local (Next.js loads it when building/running the web app).
# - Backend: packages/backend/.env.local (Convex).
# - Root .env.local is optional (e.g. for reference); Turbo passes vars via globalPassThroughEnv when set in the environment (e.g. CI).

# =============================================================================
# CONVEX
# =============================================================================
# Get these from: https://dashboard.convex.dev
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
CONVEX_DEPLOY_KEY=prod:your-deploy-key

# =============================================================================
# CLERK AUTHENTICATION
# =============================================================================
# Get these from: https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Clerk URLs (customize if needed)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# =============================================================================
# OPENCLAW (Optional - for runtime service)
# =============================================================================
# Get these from: https://openclaw.ai
# OPENCLAW_API_KEY=your-openclaw-key

# =============================================================================
# RUNTIME SERVICE (Only needed for apps/runtime)
# =============================================================================
# These are set per-droplet when deploying runtime servers
# ACCOUNT_ID=convex-account-id
# SERVICE_TOKEN=generated-service-token
# DROPLET_ID=digitalocean-droplet-id
# DROPLET_IP=droplet-ip-address
# DROPLET_REGION=nyc1
