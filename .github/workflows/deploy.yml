# Convex backend only; web app is deployed by Vercel (repo connection).
name: Deploy

on:
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy-convex:
    name: Deploy Convex Backend
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Deploy Convex
        run: npx convex deploy --cmd 'npm run build'
        working-directory: packages/backend
        env:
          CONVEX_DEPLOY_KEY: ${{ secrets.CONVEX_DEPLOY_KEY }}

  # Deploy runtime (per-account service): uncomment and configure when ready.
  # deploy-runtime:
  #   name: Deploy Runtime
  #   runs-on: ubuntu-latest
  #   needs: deploy-convex
  #   environment: production
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version-file: ".nvmrc"
  #         cache: "npm"
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     # Example: build and push runtime image, or deploy to your runtime host (DO/Fly/etc.)
  #     # - name: Build runtime image
  #     #   run: docker build -t your-registry/runtime:${{ github.sha }} -f apps/runtime/Dockerfile .
  #     # - name: Push and deploy
  #     #   run: ...
  #     #   env:
  #     #     REGISTRY_TOKEN: ${{ secrets.REGISTRY_TOKEN }}
  #     #     ACCOUNT_ID: ${{ secrets.RUNTIME_ACCOUNT_ID }}
  #     #     ...
