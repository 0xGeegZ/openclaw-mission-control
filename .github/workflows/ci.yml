name: CI

on:
  push:
    branches: [master, dev]
  pull_request:
    branches: [master, dev]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-typecheck:
    name: Lint & Typecheck
    runs-on: ubuntu-latest
    env:
      SKIP_CONVEX_TYPEGEN: "true"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Generate seed skills content
        run: npm run -w @packages/backend seed-skills:generate

      - name: Run linter
        run: npm run lint

      - name: Regenerate Convex types (if auth available)
        working-directory: packages/backend
        run: npx convex codegen 2>/dev/null || echo "Convex auth not available; skipping codegen (types will be stale)"
        continue-on-error: true

      - name: Run typecheck
        run: npm run typecheck

  test:
    name: Test
    runs-on: ubuntu-latest
    env:
      SKIP_CONVEX_TYPEGEN: "true"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run tests with coverage
        run: npm run test:all -- --coverage

      - name: Check coverage thresholds
        run: |
          echo "✓ Tests passed with coverage thresholds enforced:"
          echo "  Backend: ≥70% lines, functions, statements; ≥60% branches"
          echo "  Frontend: ≥50% lines, functions, statements; ≥40% branches"
          echo "  Runtime: ≥60% lines, functions, statements; ≥50% branches"
        if: success()

      - name: Run test coverage
        run: npx turbo run test:coverage

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint-and-typecheck, test]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build all packages
        run: npm run build
        env:
          SKIP_CONVEX_TYPEGEN: "true"
          # Repository secrets
          NEXT_PUBLIC_CONVEX_URL: ${{ secrets.NEXT_PUBLIC_CONVEX_URL }}
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
