{
  "name": "@packages/backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "convex dev",
    "setup": "convex dev --until-success",
    "seed": "convex run seed:runInternal",
    "seed-skills:download": "npx tsx scripts/seed-skills-download.ts",
    "seed-skills:generate": "npx tsx scripts/seed-skills-generate.ts",
    "seed-skills:sync": "npm run seed-skills:download && npm run seed-skills:generate",
    "build": "node -e \"if (process.env.SKIP_CONVEX_TYPEGEN === 'true') process.exit(0); require('child_process').execSync('npx convex codegen', {stdio: 'inherit'})\"",
    "typecheck": "tsc --noEmit -p convex",
    "test": "vitest run"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@packages/shared": "*",
    "convex": "^1.31.7",
    "openai": "^6.9.1"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^4.0.18"
  }
}
